[project]
name = "bugpilot-backend"
version = "0.1.0"
description = "Add your description here"
requires-python = ">=3.12"
dependencies = [
    "fastapi>=0.141.1",
    "playwright>=1.63.0",
    "pytest-playwright>=0.9.0",
    "uvicorn>=0.53.0",
]
